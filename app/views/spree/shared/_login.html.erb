<%= form_for Spree::User.new, :as => :spree_user, :url => spree.create_new_session_path do |f| %>
  <div id="password-credentials">
    <p>
      <%= f.email_field :email, :class => 'form-control', :tabindex => 1, autofocus: true, placeholder: 'Email', label: false %>
    </p>
    <p>
      <%= f.password_field :password, :class => 'form-control', :tabindex => 2, placeholder: 'Password', label: false %>
    </p>
  </div>
  <p>
    <%= f.check_box :remember_me, :tabindex => 3 %>
    <%= f.label :remember_me, Spree.t(:remember_me) %>
  </p>

  <p><%= f.submit Spree.t(:login), :class => 'btn btn-lg btn-success btn-block', :tabindex => 4 %></p>
<% end %>
